import { Container } from 'src/app/editor2/styled';
import styled from 'styled-components';

export const Wrapper = styled("div")({
    width: "100%",
    height: "100%",

    [`${Container}`]: {
        height: "100%"
    }
})